---
description: Fast health check — run the deterministic pass of every convention this project adopted.
---

Open [../skills/project-intent/references/check.md](../skills/project-intent/references/check.md)
of the `project-intent` skill and follow it.

Mechanical findings only: dead links, invalid frontmatter, a stale index, documents presumed
stale, a dubious git state. Report them, fix only what is trivial and unambiguous.

Safe to run in a loop. Anything needing judgement belongs to `/audit-project`.
